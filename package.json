{
  "name": "hotel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec ts-node src/main.ts",
    "build": "tsc",
    "start": "node dist/src/main.js",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "typecheck": "tsc --noEmit",
    "docker:compose:up": "docker compose up -d",
    "docker:compose:up:build": "docker compose up -d --build",
    "docker:compose:down": "docker compose down -v",
    "docker:compose:exec:backend": "docker compose exec backend bash",
    "docker:dev": "docker compose exec backend npm run dev",
    "docker:build": "docker compose exec backend npm run build",
    "docker:start": "docker compose exec backend npm start",
    "docker:test": "docker compose exec backend npm test",
    "docker:test:watch": "docker compose exec backend npm run test:watch",
    "docker:typecheck": "docker compose exec backend npm run typecheck"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/formbody": "^8.0.2",
    "@fastify/helmet": "^13.0.2",
    "@types/pg-promise": "^0.0.28",
    "axios": "^1.15.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "fastify": "^5.8.5",
    "jest": "^30.3.0",
    "nodemon": "^3.1.14",
    "pg-promise": "^12.6.2",
    "ts-jest": "^29.4.9",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.0"
  }
}
